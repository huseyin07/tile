'use client';
import {useState} from 'react';
export function TileGrid(){const [active,setActive]=useState<number|null>(null);return <section className="section"><div className="mb-12 flex items-end justify-between"><div><p className="eyebrow">Network view</p><h2 className="mt-4 text-4xl tracking-[-.04em] sm:text-6xl">Every piece belongs<br/>to something bigger.</h2></div><span className="hidden font-mono text-xs text-blue sm:block">{active?`${active} / 1111`:'— / 1111'}</span></div><div className="grid grid-cols-[repeat(22,minmax(0,1fr))] gap-[2px] sm:grid-cols-[repeat(38,minmax(0,1fr))]" onMouseLeave={()=>setActive(null)}>{Array.from({length:304},(_,i)=><button aria-label={`Tile ${i+1} of 1111`} onFocus={()=>setActive(i+1)} onMouseEnter={()=>setActive(i+1)} key={i} className="focusable aspect-square bg-blue transition hover:scale-150 hover:bg-white" style={{opacity:.1+((i*17)%60)/100}}/>)}</div><p className="mt-4 font-mono text-[10px] text-steel sm:hidden">{active?`${active} / 1111`:'Touch a tile · 1111 total'}</p></section>}
