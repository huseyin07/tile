'use client';
import {Menu, X as Close} from 'lucide-react';import {useState} from 'react';import {siteConfig} from '@/lib/site-config';
const links=[['Collection','collection'],['Journey','journey'],['Whitelist','whitelist'],['Lore','lore'],['FAQ','faq']];
export function Header(){const [open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-ink/80 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-[1400px] items-center justify-between px-6"><a href="#top" className="focusable text-sm font-bold tracking-[.35em]">TILE</a><nav aria-label="Main navigation" className="hidden gap-8 md:flex">{links.map(([a,b])=><a className="focusable text-xs text-steel transition hover:text-white" href={`#${b}`} key={b}>{a}</a>)}<a href={siteConfig.xUrl} aria-label="TILE on X" className="focusable text-xs">𝕏</a></nav><button className="focusable md:hidden" aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<Close/>:<Menu/>}</button></div>{open&&<nav className="border-t border-line bg-ink px-6 py-5 md:hidden">{links.map(([a,b])=><a onClick={()=>setOpen(false)} className="block py-3 text-sm text-steel" href={`#${b}`} key={b}>{a}</a>)}</nav>}</header>}
