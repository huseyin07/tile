'use client';
import {Menu,X as Close} from 'lucide-react';import {useState} from 'react';import {siteConfig} from '@/lib/site-config';
const links=[['1111 TILES','collection'],['HOW IT WORKS','journey'],['GET WL','whitelist'],['LORE','lore'],['FAQ','faq']];
export function Header(){const[open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 px-3 pt-3"><div className="mx-auto flex h-16 max-w-[1360px] items-center justify-between rounded-2xl border-3 border-ink bg-cream/90 px-5 shadow-sticker backdrop-blur-xl"><a href="#top" className="focusable flex items-center gap-2 text-lg font-black tracking-[-.04em]"><span className="flex h-9 w-9 -rotate-6 items-center justify-center rounded-lg border-2 border-ink bg-pink text-white">T</span>TILE <span lang="ko" className="text-xs text-steel">타일</span></a><nav aria-label="Main navigation" className="hidden items-center gap-6 md:flex">{links.map(([a,b])=><a className={`focusable text-xs font-black transition hover:-translate-y-0.5 ${b==='whitelist'?'rounded-lg border-2 border-ink bg-yellow px-4 py-2 shadow-sticker':''}`} href={`#${b}`} key={b}>{a}</a>)}<a href={siteConfig.xUrl} aria-label="TILE on X" className="focusable rounded-lg border-2 border-ink bg-white px-3 py-2 text-xs font-black">𝕏</a></nav><button className="focusable md:hidden" aria-label="Toggle menu" onClick={()=>setOpen(!open)}>{open?<Close/>:<Menu/>}</button></div>{open&&<nav className="mx-auto mt-2 max-w-[1360px] rounded-2xl border-3 border-ink bg-cream p-4 shadow-sticker md:hidden">{links.map(([a,b])=><a onClick={()=>setOpen(false)} className="block rounded-xl px-3 py-3 text-sm font-black hover:bg-yellow" href={`#${b}`} key={b}>{a}</a>)}</nav>}</header>}
