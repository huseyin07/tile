import {NextResponse} from 'next/server';import {adminToken,validPassword} from '@/lib/auth';
export async function POST(req:Request){const {password}=await req.json();if(!validPassword(String(password||'')))return NextResponse.json({error:'Invalid password.'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set('tile_admin',adminToken(),{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'strict',path:'/',maxAge:60*60*8});return r}
